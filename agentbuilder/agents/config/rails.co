define user ask math question
  "What is 7 + 8?"

define flow
  user ask math question

  # Extract 1st numeric value.
  $a = ...

  # Extract 2nd numeric value.
  $b = ...

  $parsed_a = execute int_parse_action(value=$a)
  $parsed_b = execute int_parse_action(value=$b)

  $result = execute sum_operation(a = $parsed_a, b = $parsed_b)
  bot respond math question


define bot respond math question
  "The result for the math question is: $result"